# JS SVG Example

This example renders shaped text to SVG in the browser using the MoonBit JS
target.

## Build

```bash
moon build --target js
```

## Run

Serve the repository root, then open the example page:

```bash
python3 -m http.server
```

Open:

```
http://localhost:8000/src/examples/js_svg/index.html
```
